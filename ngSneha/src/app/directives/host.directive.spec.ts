import { HostDirective } from './host.directive';

describe('HostBinderDirective', () => {
  it('should create an instance', () => {
    const directive = new HostDirective();
    expect(directive).toBeTruthy();
  });
});
